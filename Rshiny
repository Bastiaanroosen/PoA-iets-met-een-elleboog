## app.R ##
library(shiny)
library(shinydashboard)

#Jurgen 4) Als gebruiker wil ik kunnen zien wat de invloed is van de waterstanden op de ligtijd, 
#zodat ik weet of en zo ja wanneer ik er rekening mee moet houden dat boten een grotere kans hebben op vertraging.

ui <- dashboardPage(
  dashboardHeader(title = "POA Ligtijden"),
  dashboardSidebar(
    
  ),
  dashboardBody(
    fluidRow(
      box(plotlyOutput("water")), box(plotlyOutput("tijd"))
    ),
    
    fluidRow(box(sliderInput("urensl", "Tijd in uren", 1,24,6,1)))
  )
)

server <- function(input, output) {
  output$water <- renderPlotly({
    water  <- ggplot(Binnenvaartjoin3_small1, aes(Binnenvaartjoin3_small1$datumtijdstart, Binnenvaartjoin3_small1$start_HAVENMEAN)) + geom_line()
  })
  
  output$tijd <- renderPlotly({
    tijden  <- ggplot(Binnenvaartjoin3_small1, aes(Binnenvaartjoin3_small1$datumtijdstart, Binnenvaartjoin3_small1$hoursberth)) + geom_line()
  })
}

shinyApp(ui, server)
